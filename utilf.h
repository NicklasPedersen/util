// these functions are appended with "_f" to avoid name collisions

int is_ws_f(char c);
int is_alpha_f(char c);
int is_num_f(char c);
int is_alnum_f(char c);
int is_id_start_f(char c);
int is_valid_id_f(char c);
char *cpy_str_f(char *start, char *end);
